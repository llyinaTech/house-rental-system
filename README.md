# House Rental System (房屋租赁管理系统)

基于 Spring Boot 3 + Vue 3 的前后端分离房屋租赁管理系统。

## 📋 项目简介

本项目旨在为房东、租客和管理员提供一个高效、便捷的房屋租赁管理平台。系统涵盖了从房源发布、合同签署、租金账单管理到报修处理的全流程业务。

### 主要特性

*   **多角色支持**：管理员、房东、租客三种角色，权限隔离。
*   **房源管理**：房东发布房源，管理员审核，支持图片上传（阿里云 OSS）。
*   **合同管理**：在线生成租赁合同，支持 PDF 预览与下载，合同到期自动提醒。
*   **财务管理**：租金账单自动生成（支持月付/季付），逾期提醒，收支统计仪表盘。
*   **报修服务**：租客在线报修，房东接单处理，全流程状态追踪。
*   **消息通知**：站内信通知（合同到期、账单提醒），系统公告发布。
*   **数据统计**：可视化仪表盘展示房源出租率、财务收支趋势等。

## 🛠️ 技术栈

### 后端 (Backend)
*   **核心框架**: Spring Boot 3.5.6
*   **ORM 框架**: MyBatis-Plus 3.5.9
*   **安全框架**: Spring Security + JWT
*   **数据库**: MySQL 8.0
*   **对象存储**: 阿里云 OSS
*   **工具库**: Lombok, Hutool, Apache POI (Excel导出)

### 前端 (Frontend)
*   **核心框架**: Vue 3.5
*   **构建工具**: Vite 4.5
*   **UI 组件库**: Element Plus
*   **状态管理**: Pinia
*   **路由管理**: Vue Router 4
*   **HTTP 客户端**: Axios
*   **图表库**: ECharts 6.0

## 📂 目录结构

```
house-rental-system/
├── backend/                # 后端项目根目录
│   └── house-rental-backend/
│       ├── src/main/java   # Java 源代码
│       └── src/main/resources
│           ├── application.yml # 配置文件
│           └── sql/        # 数据库脚本
├── frontend/               # 前端项目根目录
│   └── house-rental-frontend/
│       ├── src/            # Vue 源代码
│       └── vite.config.js  # Vite 配置
└── scripts/                # 辅助脚本
```

## 🚀 快速开始

### 前置要求
*   JDK 17+
*   Node.js 16+
*   MySQL 8.0+
*   Maven 3.6+

### 1. 数据库配置
1.  创建数据库 `house_rental`。
2.  执行 `backend/house-rental-backend/src/main/resources/sql/` 目录下的 SQL 脚本：
    *   先执行 `schema.sql` (表结构)
    *   再执行 `sys_dict.sql`, `permission_data.sql`, `sys_message.sql` 等数据脚本。
3.  修改 `backend/house-rental-backend/src/main/resources/application.yml` 中的数据库连接信息：
    ```yaml
    spring:
      datasource:
        url: jdbc:mysql://localhost:3306/house_rental?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
        username: your_username
        password: your_password
    ```

### 2. 后端启动
```bash
cd backend/house-rental-backend
mvn clean package
java -jar target/house-rental-backend-0.0.1-SNAPSHOT.jar
# 或直接在 IDE 中运行 HouseRentalBackendApplication.java
```
后端服务默认启动在 `8081` 端口。

### 3. 前端启动
```bash
cd frontend/house-rental-frontend
npm install
npm run dev
```
前端服务默认启动在 `http://localhost:5173`。

## 🔑 默认账号

| 角色 | 用户名 | 密码 | 说明 |
| :--- | :--- | :--- | :--- |
| 管理员 | admin | 123456 | 拥有系统所有权限 |
| 房东 | landlord | 123456 | 测试用房东账号 |
| 租客 | tenant | 123456 | 测试用租客账号 |

## ⚙️ 核心配置说明

*   **阿里云 OSS**: 在 `application.yml` 中配置 `aliyun.oss` 相关参数以启用文件上传功能。
*   **定时任务**: 系统默认开启定时任务，每天上午 10 点检查合同到期及生成租金账单。可在 `ContractTask.java` 中修改 cron 表达式。

## 📝 开发规范
*   **代码风格**: 遵循阿里巴巴 Java 开发手册。
*   **接口规范**: 统一使用 `Result<T>` 包装返回结果。
*   **异常处理**: 全局异常处理器 `GlobalExceptionHandler` 统一处理业务异常。

## 📄 License
MIT License
